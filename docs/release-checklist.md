> 2026-09-23 状态更新：项目已选择 AGPL-3.0-only 并公开自身源码。下文为原候选构建的历史材料，其中“尚未选择许可”的表述已由 [开源决定](../docs/OPEN_SOURCE.md) 更新；第三方对应源码材料和二进制验收仍待完成，安装包保持草稿。

# 学习模型 Windows 发布检查表

适用版本：0.1.0 及后续版本。每次候选构建复制本表，填写版本、构建者、日期、机器/虚拟机快照与证据路径。未勾选项不得按“已验证”对外说明。

> **公开发布阻断：** 当前构建包含 PyMuPDF 1.27.2.3。项目尚未形成并审核通过 AGPL 合规分发方案，也未记录适用的 Artifex 商业许可。此项解决前，只能生成内部/QA 候选包，不得公开上传或分发。收集许可证文本不等于解决阻断。

## 1. 版本与构建输入

- [ ] 应用版本、`study_app/app_metadata.py`、`packaging/version_info.txt`、安装器版本和文件名一致。
- [ ] AppId 仍为 `{E06D2BBE-5FA4-4F72-B03E-130652DE5A52}`，没有因升级而改变。
- [ ] 使用 Windows x64、CPython 3.14.5 x64 和独立 `.venv-build`。
- [ ] `requirements-build.lock.txt` 中所有直接依赖版本均与构建环境一致。
- [ ] 正式多尺寸 `study_app/ui/assets/app.ico` 已审核，不是占位图标。
- [ ] Git 工作区和候选提交已记录；没有把未审查的本地改动混入构建。
- [ ] 已确认本版更新记录和用户可见版本说明。

## 2. 自动测试、构建和白名单

- [ ] 执行 `pwsh -File tools/build_release.ps1`，没有使用 `-SkipTests`。
- [ ] 全部 pytest 测试通过，日志已保存。
- [ ] PyInstaller 以 `onedir` 生成 `dist/LearningModel/LearningModel.exe`。
- [ ] `tools/verify_release.ps1 -RunSelfTest` 通过。
- [ ] 自检使用临时用户目录成功创建干净数据库、默认模型和空记录集。
- [ ] `dist` 顶层仅有 `LearningModel.exe` 与 `_internal`。
- [ ] 打包资源仅包括明确列入 `packaging/LearningModel.spec` 的 SQL、默认 JSON、SVG 与桌面桥接脚本。
- [ ] `dist` 不含 `app_data`、根目录 `learning_model_v1.json`、根目录 `learning_records.json`、测试、缓存、日志、备份、密钥、个人教材或作业。
- [ ] 从快捷方式和非项目工作目录启动均正常，安装目录只读时仍可保存数据。

## 3. 许可与安全

- [ ] `tools/collect_licenses.py` 成功，`THIRD_PARTY_LICENSES/LICENSE-MANIFEST.json` 与锁定版本一致。
- [ ] 所有运行时组件的原始许可证/NOTICE 文件均随发布材料提供。
- [ ] PySide6/Qt、Shiboken、Pillow、ReportLab、pypdf、pytesseract 的分发义务已按本次实际二进制重新审查。
- [ ] **PyMuPDF AGPL/商业许可阻断已由负责人书面关闭，并附合规依据。**
- [ ] 未发现 API 密钥、访问令牌、私钥、个人路径、数据库或学习资料。
- [ ] 外部 AI 服务密钥由用户配置，安装包不预置凭据。
- [ ] 若有诊断数据收集，发布页已说明内容、保存位置和用户控制方式；否则明确说明不自动上传。

## 4. 代码签名与安装器

- [ ] 发布证书用途和保管流程已确认。
- [ ] 先签名 `LearningModel.exe`，签名和时间戳均有效。
- [ ] 使用 Inno Setup 6.3 或更高版本生成 `LearningModel-Setup-<版本>-x64.exe`。
- [ ] 安装器为当前用户安装，普通用户不需要管理员权限。
- [ ] 安装目录为 `%LOCALAPPDATA%\Programs\LearningModel`。
- [ ] 安装器及卸载器不会删除 `%LOCALAPPDATA%\LearningModel` 用户数据。
- [ ] 安装程序检测到应用运行时提示正常关闭，没有强杀正在写数据库的进程。
- [ ] 安装器已签名，签名和时间戳均有效。
- [ ] 对最终签名后的安装器生成 SHA-256；生成校验和后没有再次修改安装器。

## 5. 干净 Windows 10+ x64 验收

记录环境：Windows 版本 ______；显示缩放 ______；用户名/路径 ______；快照 ______。

- [ ] 普通用户首次安装、开始菜单启动和可选桌面快捷方式正常。
- [ ] 在无 Python、Git、开发工具的机器上可启动。
- [ ] 中文用户名、中文路径、离线状态均通过。
- [ ] 主页、计划、学习助理、图谱、设置均可进入和操作。
- [ ] 菜单首次收起、连续展开/收起、页面切换、窗口缩放通过。
- [ ] 100%、125%、150% 显示缩放通过，无关键控件遮挡。
- [ ] 记录冷启动、首次收起菜单和后续收起菜单耗时；无新增明显卡顿。
- [ ] 学科引入、预览、确认、可恢复移除和恢复通过；失败场景无部分写入。
- [ ] 计划预览/确认、完成任务、退出重启后状态一致。
- [ ] 无密钥、无网络、无 Tesseract、无 ChatGPT 桌面端时，核心流程仍可使用且可选能力提示清楚。
- [ ] 初始化/迁移失败、目录不可写、备份失败均保留原数据并产生可定位日志。

## 6. 升级、卸载与回滚

- [ ] 从上一公开版本安装并创建有代表性的数据集。
- [ ] 覆盖安装识别为升级，数据库升级前创建备份。
- [ ] 升级后记录数量、学科状态、计划、设置和关键关联正确。
- [ ] 中断一次迁移后可恢复，原数据库未损坏。
- [ ] 旧版面对不兼容的新数据库会停止写入并提示，不会破坏数据。
- [ ] 卸载只删除程序文件，用户数据保留。
- [ ] 卸载后重装仍可读取保留数据。
- [ ] 若测试清除个人数据，已使用独立、明确、有备份提示的操作；不是卸载器默认行为。

## 7. 发布与下载回验

- [ ] 发布目录只含安装器、`SHA256SUMS.txt`、安装/升级说明、更新记录、第三方许可说明及许可证文件。
- [ ] 下载页写明版本、Windows 10+ x64、变更、数据位置、限制和反馈方式。
- [ ] 上传目标权限已复核，没有误发到公开渠道。
- [ ] 从最终下载链接重新下载，SHA-256 与发布值一致。
- [ ] 下载回来的文件在干净环境完成一次安装和启动。
- [ ] 发布证据、截图、日志、哈希和签署人记录已归档。

## 签署

| 角色 | 姓名 | 日期 | 结论/证据 |
| --- | --- | --- | --- |
| 构建 |  |  |  |
| 功能验收 |  |  |  |
| 数据迁移验收 |  |  |  |
| 安全与隐私 |  |  |  |
| 开源/商业许可 |  |  |  |
| 发布批准 |  |  |  |

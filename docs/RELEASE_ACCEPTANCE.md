# 0.1.0 预发布验收

验收：2026-09-23；收尾：2026-09-24。定位：初步测试版，保持 GitHub Release 草稿。

## 实测范围

Windows 11 Enterprise Evaluation 25H2、标准用户、VirtualBox 7.2.16；未安装开发环境。自动键盘输入受虚拟机限制，文字由用户填写，其余通过真实界面鼠标操作及数据库核对完成。

- 安装、打包自检、启动及页面切换通过。
- 学科引入、可恢复移除、恢复通过；状态 active → archived → active，知识点完整、数据库完整性检查通过。该数据集没有学习记录，非空记录保留仅由自动测试覆盖。
- 60 分钟空计划预览和保存通过；独立合成配置下，20 分钟任务的预览、确认、完成保存、重复预览保留完成状态通过。没有伪造真实学习成绩。
- 侧栏图标、展开收起过渡、窗口最大化及还原检查通过。
- 前一候选包实际卸载验证：默认保留数据、重装、取消清除、确认清除均符合预期；清除只针对当前用户应用数据，外部文件保留。本次保留同一卸载实现，并对最终包补做冒烟验证。

## 已知限制

- 新引入且没有学习覆盖的知识点不会自动成为计划候选；下一步引导仍可改进。
- 未完成 Windows 10、多档系统 DPI、真实做题分析/评分、外部 AI 服务及 OCR 的端到端验收。
- 性能日志样本见到启动事件循环延迟约 127.9 ms、操作期间约 52.5 ms；没有逐帧或函数级 profiler 证据，不声称首次收起卡顿的根因已消除。
- 虚拟机正常重启仍存在宿主兼容问题，使用已登录会话完成验收，未关闭宿主安全功能。
- 安装包未签名；卸载清除不承诺删除 Windows 系统日志或用户自行保存的安装包、导出文件。

## 最终构建

- CPython 3.14.5 x64、Inno Setup 7.1.0；按锁定依赖从发布暂存源码重新构建。
- 完整测试：1072 passed，279 subtests passed，5 条非失败警告；未跳过测试。
- 冻结程序自检和安装包结构/版本检查通过。
- 117 个项目 Python 输入已按最终 PyInstaller Analysis 清单记录；79 个原生 DLL/PYD 与前一候选组件哈希一致。
- 安装包 SHA-256：`46193d253e1f0c55b082c116dbde1c5e2c314aaa654f20a78f5276abea27cd2e`。

## 最终包虚拟机复验

覆盖安装、静默卸载保留数据、重装均成功，三个阶段的原数据库 SHA-256 均未变化；卸载后程序文件已移除。最终程序自检返回 status=ok、version=0.1.0。使用独立空配置进行真实界面冒烟检查，学习计划空时间点击预览显示中文校验信息，未生成计划。
